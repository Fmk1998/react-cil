import React, {FunctionComponent} from "react"import {Drawer, List, ListItem, ListItemIcon, ListItemText, Divider, Typography, Avatar} from "@material-ui/core"import {Inbox, Mail} from "@material-ui/icons"import "./index.scss"const SlideBar: FunctionComponent = () => {    return(        <Drawer variant="permanent" anchor="left" className="slide">            <Typography variant="h5" className="logo">                <img src={require("../../../assets/logo.png")} alt=""/>                <span>React-cli</span>            </Typography>            <Divider />            <List>                {['菜单1', '菜单2', '菜单3'].map((text, index) => (                    <ListItem button key={text}>                        <ListItemIcon>{index % 2 === 0 ? <Inbox /> : <Mail />}</ListItemIcon>                        <ListItemText primary={text} />                    </ListItem>                ))}            </List>        </Drawer>    )}export default SlideBar;